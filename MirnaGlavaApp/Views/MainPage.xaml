<ContentPage
    x:Class="MirnaGlavaApp.Views.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:MirnaGlavaApp.ViewModels">

    <ContentPage.BindingContext>
        <viewmodels:MainViewModel />
    </ContentPage.BindingContext>

    <Grid ColumnDefinitions="220,*">
        <!-- Leva strana -->
        <ScrollView Grid.Column="0" BackgroundColor="#f0f0f0" Padding="10">
            <VerticalStackLayout>
                <Label Text="Lists" FontSize="20" FontAttributes="Bold" Margin="0,10" />
                <CollectionView ItemsSource="{Binding TaskLists}"
                                SelectionMode="Single"
                                SelectedItem="{Binding SelectedList}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame Margin="5" Padding="5" BackgroundColor="#fff">
                                <Label Text="{Binding Name}" />
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Entry Placeholder="New list..." Text="{Binding NewListName}" />
                <Button Text="Add List" Command="{Binding AddListCommand}" />
            </VerticalStackLayout>
        </ScrollView>

        <!-- Desna strana -->
        <VerticalStackLayout Grid.Column="1" Padding="15">
            <Label Text="{Binding SelectedList.Name}" FontSize="24" FontAttributes="Bold" />
            <Entry Placeholder="Add task..." Text="{Binding NewTaskTitle}" />
            <Button Text="Add Task" Command="{Binding AddTaskCommand}" />

            <CollectionView ItemsSource="{Binding Tasks}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout Padding="5">
                            <CheckBox IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                CheckedChanged="OnTaskCheckedChanged" />
                            <Label Text="{Binding Title}" VerticalOptions="Center"/>
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </Grid>
</ContentPage>
